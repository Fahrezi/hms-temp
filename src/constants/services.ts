const USER_SERVICE = {
  BASE: '/users',
};

const AUTH_SERVICE = {
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
  LOGOUT: '/auth/logout',
};

const RESERVATION = {
  BASE: '/reservations',
}

export { USER_SERVICE, AUTH_SERVICE, RESERVATION };
